package farm;

import static org.junit.jupiter.api.Assertions.assertTrue;

import org.junit.jupiter.api.Test;

public class HttpMessageParserTest {
	@Test
	public void test() {
		// Cannot instantiate bellow type HttpMessageParser
		// HttpMessageParser parser = new HttpMessageParser(null, false);

		assertTrue(true);
	}
}
